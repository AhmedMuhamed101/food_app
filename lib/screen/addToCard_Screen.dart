import 'package:flutter/material.dart';

class AddToCardScreen extends StatelessWidget {
  const AddToCardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Add To Card"),
    );
  }
}